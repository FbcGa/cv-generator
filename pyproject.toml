[project]
name = "cv"
version = "0.1.0"
description = "This is a project for build a custom cv"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=25.2.0",
    "weasyprint>=68.1",
]
